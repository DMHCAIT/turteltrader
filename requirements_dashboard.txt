# Dashboard-specific Dependencies
# Note: Install main requirements.txt first, then these additional dashboard dependencies

streamlit>=1.28.0

# Enhanced plotting for dashboard
plotly>=5.15.0

# Additional dashboard utilities (if not in main requirements)
streamlit-plotly-events>=0.0.6
